# Runtime dependencies for the TaskFlow API.
# Everything else used (sqlite3, hashlib via werkzeug.security) is stdlib
# or a Flask dependency, so the footprint stays intentionally small.
Flask>=3.0
PyJWT>=2.8
Werkzeug>=3.0
